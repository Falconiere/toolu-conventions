# Cloudflare deployment identity. Merge these names into .env.example; keep
# their real values in an ignored .env locally and the CI secret store remotely.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
