id: no-bare-fetch-tsx
language: tsx
severity: error
message: bare fetch() — call the configured http client instead, so auth headers, baseUrl, timeouts and error shaping stay in one place
ignores:
  - "**/utilities/http.ts"
  - "**/__tests__/**"
rule:
  pattern: fetch($$$ARGS)
