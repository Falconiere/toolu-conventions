# drizzle-kit runs in Node, outside the Worker, so it reads real env vars rather
# than bindings. The RUNTIME never uses these — src/constants/env.ts parses the
# values the API passes in. Copy to .env for migrations only.
TURSO_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=
