---
/** `/` — the home page. A page composes sections; it holds no markup of its own. */
import BaseLayout from '@/layouts/base-layout.astro';
import HeroSection from '@/sections/hero-section.astro';
---

<BaseLayout title="{{TOOLU_DISPLAY_NAME_HTML}}" description="{{TOOLU_DESCRIPTION_HTML}}">
  <HeroSection />
</BaseLayout>
