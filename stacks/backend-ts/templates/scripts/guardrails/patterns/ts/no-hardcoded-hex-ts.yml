id: no-hardcoded-hex-ts
language: typescript
severity: error
message: hardcoded hex colour — use a token from ui/theme/colors.ts so light/dark and a palette change stay one edit, not a search
ignores:
  - "**/ui/theme/**"
  - "**/__tests__/**"
rule:
  kind: string
  regex: '^.#[0-9a-fA-F]{3,8}.$'
