# <folder-name>/

> One paragraph: what belongs in this folder and what does NOT. Keep it short —
> this is the index an agent reads before adding or searching for code here.

**Purpose:** <what-this-folder-is-for>

**Put here:** <what-goes-here>
**Don't put here:** <what-does-not> (→ goes in `<where-instead>`)

## What's inside

<!-- One line per notable file/subfolder. Update when you add something reusable. -->

- `example.ts` — <one-line-description>

## Conventions

- No barrel files — import the concrete file via its `@/` alias.
- Kebab-case filenames named after the export; one primary export per file.
- Co-locate tests in `__tests__/`.
