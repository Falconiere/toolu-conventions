# GitHub Actions — code-quality gate for every PR (and pushes to main).
# Mirrors `bun run check` step for step: type-check + lint + format-check +
# structure + knip + jscpd + test. Builds, OTAs, and submissions live in EAS
# Workflows (.eas/workflows/*), not here.
#
# Copy to `.github/workflows/ci.yml`. Keep the Bun version in sync with the `bun`
# field in eas.json.
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# A newer push to the same ref cancels the older in-flight run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: type-check · lint · format · structure · unused · dupes · test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14 # keep in sync with eas.json `bun`

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type-check
        run: bun run type-check

      - name: Lint
        run: bun run lint

      - name: Format check
        run: bun run fmt:check

      - name: Structure check
        run: bun run check:structure

      - name: Unused files, exports, dependencies (knip)
        run: bun run check:unused

      - name: Copy-paste detection (jscpd)
        run: bun run check:dupes

      - name: Test
        run: bun run test
